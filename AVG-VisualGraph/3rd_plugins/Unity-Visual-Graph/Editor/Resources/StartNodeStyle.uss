/* Add a round border to all nodes */
/*开始节点Style*/
.VisualGraphNode {
    border-radius: 10;
    background-color: rgba(255, 0, 0, 1);
}


/* Change the background color of the title section */
#title {
    background-color: rgba(1, 0, 0, 1);
}